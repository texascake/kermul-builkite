# .buildkite/pipeline.yml
steps:
  - label: ":pipeline:"
    command: "buildkite-agent pipeline upload"
    timeout_in_minutes: 30
    agents:
      queue: "default"
    secrets:
      - TG_CHAT_ID
      - TG_TOKEN
