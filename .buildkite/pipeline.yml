# .buildkite/pipeline.yml
steps:
  - label: "🚀 Build Kernel (X00TD)"
    command: "bash scripts/build.sh"
    timeout_in_minutes: 30
    agents:
      queue: "kernel-builder-machine"
    env:
      TG_TOKEN: ${TG_TOKEN}
      TG_CHAT_ID: ${TG_CHAT_ID}
      TG_TOPIC_ID: ${TG_TOPIC_ID}
